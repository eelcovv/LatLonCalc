name: latloncalc_env
channels:
  - conda-forge
dependencies:
  - python>=3.6
  - pip
  - tox
  - pytest
  - pyproj
  - numpy
  - pip:
      - .  # install git checkout of demo-dsproject in editable mode
